version: "3.9"

services:

  postgres:
    image: postgres:latest

    environment:
      POSTGRES_USER: sagar
      POSTGRES_PASSWORD: sagar
      POSTGRES_DB: emos

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: